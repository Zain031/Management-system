spring.application.name=ManagementSystem
server.port=${APP_PORT}

#Database Config

spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.url=jdbc:postgresql://${DB_URL}:${DB_PORT}/${DB_NAME}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true

#JWT Configuration Secret
app.tokonyadia.jwt.secret-key=${JWT_SECRET_KEY}
app.tokonyadia.jwt.app-name=${JWT_APP_NAME}
app.tokonyadia.jwt.expire-time=${JWT_EXPIRE_TIME}

#OpenAPI Configuration
springdoc.api-docs.path=/docs/openapi
